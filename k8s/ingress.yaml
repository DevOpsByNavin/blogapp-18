apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traefik
  namespace: blogapp
spec:
  ingressClassName: traefik
  rules:
  - http:
      paths:
      - backend:
          service:
            name: nginx
            port:
              number: 80
        path: /
        pathType: Prefix
status:
  loadBalancer: {}