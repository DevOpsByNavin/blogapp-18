apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traefik
  namespace: blogapp
spec:
  ingressClassName: traefik
  rules:
  - host: 13.232.45.191
    http:
      paths:
      - backend:
          service:
            name: nginx
            port:
              number: 80
        path: /
        pathType: Prefix
status:
  loadBalancer: {}